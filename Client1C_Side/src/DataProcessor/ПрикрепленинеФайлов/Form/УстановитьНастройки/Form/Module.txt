
&НаСервере
Процедура СохранитьНастройкиНаСервере()
	Настройки = Новый Структура("Сервер, Скрипт, Пользователь, Пароль", Объект.Сервер, Объект.Скрипт, Объект.Пользователь, Объект.Пароль);
	СтрокаСНастройками = ЗначениеВСтрокуВнутр(Настройки);
	Константы.НастройкиОбменаФайлами.Установить(СтрокаСНастройками);
КонецПроцедуры

&НаКлиенте
Процедура СохранитьНастройки(Команда)
	СохранитьНастройкиНаСервере();
	ЭтаФорма.Закрыть();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	СтрокаСНастройками = Константы.НастройкиОбменаФайлами.Получить();
	Если ЗначениеЗаполнено(СтрокаСНастройками) Тогда
		Настройки = ЗначениеИзСтрокиВнутр(СтрокаСНастройками);
		ЗаполнитьЗначенияСвойств(Объект, Настройки);
	КонецЕсли;

КонецПроцедуры
