
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если ЗначениеЗаполнено(ПараметрКоманды) Тогда
		Форма = ПолучитьФорму("Обработка.ПрикрепленинеФайлов.Форма.Форма",, ПараметрКоманды);
		Форма.ОтправитьФайл(Строка(ПараметрКоманды.УникальныйИдентификатор()));
	КонецЕсли;	
	
КонецПроцедуры

